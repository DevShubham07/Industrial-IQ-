services:
  - type: web
    name: dealerpulse
    env: node
    rootDir: dealerpulse
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
